# 감정일기 앱 개발 스펙

> 26.07.11 검토 완료판. 기획 단계의 화면 스케치 PDF(26.07.06)가 있었으나
> v1.0 공개 때 저장소에서 뺐다 — 차이가 있으면 본 문서를 우선한다.
> **26.07.12 개정: GUI를 CustomTkinter에서 PySide6(Qt)로 전환.** tkinter는 한글 IME
> 인라인 조합(조합 중 글자를 본문에 실시간 표시)을 구조적으로 지원하지 않는데,
> 자연스러운 한글 입력은 이 앱의 필수 요건이라 프레임워크를 교체한다.
> DB·서비스 계층과 테스트는 그대로 유지되고 UI 계층만 이식한다.

## 1. 프로젝트 개요

- **제작 목적**: 대학생 포트폴리오 프로젝트 (심리학과 4학년, 대학원 지원용)
- **사용 대상**: 감정 관리 및 정신 건강 관리에 어려움이 있는 사람
- **제작 배경**: 임상심리 수업에서 배운 감정 일기(치료 요법)를 바탕으로, 사람들이 쉽고 부담 없이 쓸 수 있는 감정 일기 도구를 만듦
- **이론적 배경**: 사건-감정-생각 3단 구조는 CBT(인지행동치료)의 사고기록지(thought record) 형식과 맞닿아 있음
- **특징**: 사용자 친화적 인터페이스, 직관적이고 쉬운 사용법, 보기 편한 색상, 단독 실행 가능

## 2. 기술 스택

| 구분 | 선택 | 라이선스 | 이유 |
|---|---|---|---|
| 언어 | Python 3.12 | PSF | Claude Code로 다루기 쉽고 학습 자료도 풍부함 |
| GUI | PySide6 (Qt for Python) | LGPL v3 | **한글 IME 인라인 조합 완전 지원** (전환 사유, 26.07.12). 모던 UI·내장 캘린더 위젯. ⚠️ 쌍둥이 라이브러리 PyQt6는 GPL이므로 절대 혼용 금지 |
| 데이터 저장 | SQLite (`sqlite3`, 파이썬 내장) | 퍼블릭 도메인 | 검색·정렬·필터·통계 계산에 유리하고 추가 설치 불필요 |
| 차트 | QPainter 직접 그리기 | — | ⚠️ Qt의 QtCharts 모듈은 GPL 전용이라 사용 금지. 파이 차트·그라데이션 척도는 단순 도형이라 직접 그리기로 충분 |
| PDF 내보내기 | Qt 내장 `QPdfWriter` + `QTextDocument` | LGPL v3 (PySide6에 포함) | **26.07.21 변경**: reportlab을 쓰지 않기로 함. HTML을 만들어 두면 공유용 HTML과 PDF가 같은 코드로 나오고, 외부 의존성이 PySide6 하나로 줄어든다. 대신 QTextDocument가 지원하는 CSS가 제한적이라 카드 배경·테두리는 표로 짠다 |
| 한글 폰트 (화면·PDF 공통) | 시스템 폰트 + 대체 목록 (`theme.FONT_STACK`) | — | **26.07.24 보완**: 폰트를 동봉하지 않아 재배포 대상이 아니다. 맑은 고딕이 없는 환경에서는 나눔고딕·Noto Sans KR 등 설치된 다른 한글 폰트로 Qt가 자동으로 넘어간다. 적용 지점을 `theme.qfont()`와 `theme.FONT_STACK_CSS` 둘로 모았다 — §8-2 참고 |
| 형태소 분석 | 사용 안 함 (규칙 기반 자모 매칭) | — | KoNLPy는 GPL+Java 의존으로 탈락, Kiwipiepy는 LGPL+용량 부담으로 보류. §5 참고 |
| 패키징 | PyInstaller (**onedir 폴더형**) | GPL+예외 | 부트로더 예외 조항으로 산출물은 자유 배포 가능. onedir는 LGPL '라이브러리 교체 가능성' 조건 충족용 |

- Node.js, npm, 웹뷰, 브라우저 등 외부 프로그램에 의존하지 않는 구조
- 실행 시 별도 설치 과정 없이 구동 — 배포 형태는 **폴더(zip)**: 압축 해제 후 exe 실행 (단일 exe는 LGPL 교체가능성이 애매해져 채택하지 않음)

### 라이선스 관리 체크리스트 (LGPL 대응 포함)

- 앱 소스 코드 공개 의무 **없음** (LGPL은 라이브러리에만 적용, 앱은 자유)
- 배포물 `LICENSES/` 폴더에 동봉: PySide6·Qt(LGPL v3 전문 + 고지문 + 공식 소스 저장소 링크)
  — 26.07.21 기준 외부 의존성이 PySide6뿐이라 이것만 넣으면 된다(reportlab·나눔고딕 제외)
- PyInstaller **onedir** 배포 → Qt DLL이 교체 가능한 별도 파일로 유지됨
- Qt 자체를 수정하지 않음 (수정 시에만 그 수정분 공개 의무 발생)
- **금지 목록**: PyQt6(GPL), QtCharts·QtDataVisualization 등 GPL 전용 Qt 모듈
- 표정 아이콘·앱 아이콘은 직접 그리거나 유니코드 문자 사용 (이미지 저작권 이슈 원천 차단)

## 3. 프로젝트 구조

```
감정일기/
├── main.py                        # 진입점
├── requirements.txt
├── emotion_diary.spec             # PyInstaller 설정 (9단계)
├── LICENSES/                      # 의존성 라이선스 고지문 (PySide6·Qt)
├── app/
│   ├── config.py                  # 경로, 상수
│   ├── theme.py                   # 색상 팔레트, 폰트
│   ├── db/
│   │   ├── database.py            # SQLite 연결 + 스키마 + 기본 단어 시드
│   │   ├── diary_repo.py          # diary_entries CRUD, 검색·정렬·필터, 통계용 집계
│   │   ├── emotion_repo.py        # emotion_dictionary·카테고리 CRUD
│   │   └── tag_repo.py            # entry_emotion_tags 갱신·집계
│   ├── services/
│   │   ├── conjugator.py          # 어간·활용형 규칙 생성 엔진 (인식·자동완성 공용)
│   │   ├── emotion_detector.py    # 텍스트 → 감정 단어 인식 (자모 매칭 + 부정 감지)
│   │   ├── analysis.py            # 기간별 통계
│   │   ├── backup.py              # 백업용 JSON 내보내기·복원 (26.07.20)
│   │   └── report.py              # 공유용 HTML(페이지형) / PDF 내보내기
│   ├── data/
│   │   └── default_emotions.py    # 기본 감정 단어 시드 (어간 수동 검수 포함)
│   └── ui/                        # PySide6 (Qt) — 이식 기간에는 ui_qt/로 병행 후 교체
│       ├── app_window.py          # 메인 윈도우, 화면 전환
│       ├── style.py               # QSS 스타일시트 (theme.py 색상 사용)
│       ├── screens/               # home, editor(화면2+3 통합), diary_list,
│       │                          # diary_calendar, analysis, dictionary
│       └── widgets/               # mood_slider, word_picker,
│                                  # guide_popup, mood_gauge
└── tests/
```

- 설계 원칙: UI ↔ 로직(services) ↔ 저장(db) 3계층 분리
- 화면2(작성)·화면3(상세보기)은 하나의 editor 컴포넌트에서 쓰기/보기 모드 분기

## 4. 데이터 구조 (확정)

**diary_entries** (일기 항목)
- id, date (`YYYY-MM-DD`), title
- mood_scale (정수, -10 ~ +10)
- mode ("template" | "free")
- event_text / emotion_text / thought_text (템플릿 모드일 때)
- free_text (자유 작성 모드일 때)
- is_favorite (참/거짓)
- created_at, updated_at

**emotion_categories** (감정 카테고리)
- id, name, sort_order, is_default
- 기본 5개(기쁨/슬픔/분노/불안/기타) 제공, 사용자가 추가·편집 가능

**emotion_dictionary** (감정 단어집)
- id, category, word (기본형), meaning
- **stems**: 매칭용 어간 목록 (쉼표 구분). 기본 단어는 불규칙 활용까지 수동 검수, 사용자 추가 단어는 규칙 엔진이 자동 생성 + 직접 수정 가능
- is_default (기본 제공 / 사용자 추가 구분)
- 현재는 한국어만 지원(추후 영어 확장 가능성 있음)

**entry_emotion_tags** (일기에서 인식된 감정 단어 기록)
- id, entry_id, word, category (텍스트 스냅숏 — 단어집을 나중에 수정해도 과거 분석 기록 보존)
- **count**: 해당 일기 안에서의 등장 횟수 (최다 사용 단어 통계 정확도용)
- 일기 저장·수정 시마다 갱신 (기존 기록 삭제 후 재삽입)
- 분석 기능은 일기 텍스트를 다시 훑지 않고 이 표를 집계

**meta**
- schema_version (JSON 백업 호환성·마이그레이션 대비), 첫 실행 여부 등

## 5. 감정 단어 인식·자동완성 알고리즘 (확정)

### 5-1. 어간 규칙 생성 엔진 (`conjugator.py`)
- 형태소 분석기 없이, 한국어 용언 활용의 대표 규칙을 문자열 변환으로 구현
  - "~하다" → "하다" 제거 (행복하다 → 행복)
  - "~다" 제거 + 과거형 (설레다 → 설레, 설렜)
  - ㅂ 불규칙 (무섭다 → 무섭, 무서우)
  - ㅡ 탈락 (슬프다 → 슬프, 슬퍼)
- 같은 엔진을 **① 인식용 어간 생성**과 **② 자동완성 활용형 생성** 두 곳에 공용

### 5-2. 인식 (하이라이트 + 태그)
- **자모 단위 어간 매칭**: 텍스트와 어간을 초성·중성·종성으로 분해해 접두 일치 검사. 음절 단위로는 못 잡는 "슬픔"(슬프+ㅁ), "서러움"(서러우+ㅁ) 등을 커버하고, 단어당 필요 어간 수도 1~2개로 줄어듦
- **부정 감지**: 매칭 직후 "지 않"/"지 못", 직전 "안 "/"못 "이 있으면 태그 집계에서 제외 ("행복하지 않았다"가 기쁨으로 집계되는 오류 방지)
- **디바운스**: 실시간 하이라이트는 키 입력마다가 아니라 타이핑 멈춤 0.3초 후 1회 검사
- 하이라이트 구현은 Qt의 QSyntaxHighlighter 사용 (코드 에디터 키워드 표시와 같은 원리)
- 한계 수용: 오타·변칙 표기는 미인식. 안전망으로 단어별 stems를 사용자가 편집 가능

### 5-3. 자동완성 (화면2)
- **호출**: 입력칸에서 Ctrl+Space → 커서 위치에 단어집 팝업 (카테고리별 + 필터 입력칸)
- **스마트 필터**: 커서 앞에 쓰다 만 글자가 있으면 그걸로 목록 선필터 ("서" → 서럽다…)
- **선택**: 방향키+Enter 또는 클릭, Esc로 닫기
- **형태 변환**: 단어 선택 시 활용형 7개를 제시, 하나를 골라 삽입 (쓰다 만 글자는 대체)

| 기본형 | 과거형 | 관형형 | 연결형 | 명사형 | 의문형 | 변화형 |
|---|---|---|---|---|---|---|
| 서럽다 | 서러웠다 | 서러운 | 서러워서 | 서러움 | 서러울까 | 서러워졌다 |

- 부정형("~지 않았다")은 목록에 넣지 않음 (5-2 부정 감지와 일관성)
- 삽입된 단어는 즉시 하이라이트 인식됨

## 6. 화면별 스펙

### 화면1 · 홈
- 최근 작성한 일기 최대 5개 미리보기 (제목·날짜·척도만 간단히 표시)
- 버튼 4개: 새 일기 작성 / 적은 일기 보기 / 분석 보기 / 감정 단어집
- 전체 검색·정렬 기능은 화면4에만 두고 홈에서는 중복하지 않음
- 백업/내보내기: 전체 일기를 내보내는 기능 (작은 아이콘/메뉴로 배치). 내보낼 때 형식 선택
  - **JSON**: 순수 데이터 백업용 — diary_entries 전체 + emotion_dictionary(사용자 추가분 포함) + 카테고리 + schema_version 포함 (추후 재가져오기·기기 이전 대비)
  - **PDF**: 사람이 읽기 편한 형식 (Qt `QPdfWriter`). 1쪽 분석, 2쪽 목차(제목이 해당
    일기로 가는 링크), 그 뒤로 한 쪽에 일기 하나
  - **HTML**: 앱 화면에 가장 가까운 형식. 왼쪽 사이드바로 넘겨 보며, 1쪽 분석,
    2쪽 적은 일기 보기, 그 뒤로 한 쪽에 일기 하나 (이미지 내장으로 파일 하나 완결)

### 화면2 · 일기 작성
- 제목, 날짜, 가이드 버튼(?), 즐겨찾기 등록(☆)
- 기분 척도: -10 ~ +10, 슬픔·중립·기쁨 표정 아이콘과 함께 표시
- 템플릿 작성(사건 / 감정 / 생각) ↔ 자율 작성, 상단 토글로 선택
- 감정 단어 자동인식: §5-2 방식으로 하이라이트, 저장 시 entry_emotion_tags 기록
- 감정 단어 자동완성: §5-3 방식 (Ctrl+Space)
- 하단 "작성완료" 버튼으로 저장

### 화면3 · 일기 상세보기
- 화면2와 동일한 레이아웃 (하나의 editor 컴포넌트에서 쓰기/보기 모드 분기)
- 하단 버튼만 "수정" / "삭제"로 교체 ("삭제" 클릭 시 확인 팝업 표시)

### 화면4-1 · 일기 목록 (리스트 뷰)
- 검색: 날짜 / 제목 / 감정 단어
- 정렬: 날짜 / 척도 (오름차순·내림차순 토글) — 기본값은 날짜 내림차순(최신순)
- 즐겨찾기 ON/OFF 필터
- 리스트/캘린더 뷰 전환
- 척도 값에 색상 표시 (양수 초록 / 중립 노랑 / 음수 빨강 계열)
- 선택 삭제(확인 팝업), 분석 보기 이동, 새 일기 작성 이동

### 화면4-2 · 일기 목록 (캘린더 뷰)
- 월간 단위 캘린더
- 날짜 검색은 제공하지 않음 (캘린더가 날짜 탐색을 겸함 — 제목·감정 단어 검색만 제공)
- 하루에 일기가 2개 이상이면 가장 먼저 작성한 일기의 제목 + "+N" 표시, 날짜 칸 클릭 시 그 날짜의 일기 목록 표시
- 즐겨찾기 표시, 분석 보기·새 일기 작성 버튼은 리스트 뷰와 동일

### 화면5 · 분석
- 기간 선택: 주간 / 월간 / 연간, 좌우 화살표로 이전·다음 기간 이동
- 감정 카테고리 파이 차트 (Canvas 직접 그리기)
- 선택 기간 내 작성한 일기 개수
- 가장 많이 사용한 감정 단어 상위 3개 (tags의 count 합산 기준)
- 척도 그래프: -10(빨강) ~ 0(노랑) ~ +10(초록) 그라데이션 스케일 + 보라색 마커 1개(기간 평균 mood_scale 위치)
- "기분이 좋은 날이 더 많았어요" 같은 문장형 요약 (긍정/부정 일수 비교)

### 화면6 · 감정 단어집
- 카테고리: 기쁨 / 슬픔 / 분노 / 불안 / 기타 (사용자가 추가·편집 가능)
- 단어는 기본형으로 저장, 자동완성 시 §5-3 규칙으로 형태 변환
- 단어 추가 시 어간(stems) 자동 생성 + 고급 옵션으로 직접 수정 가능 (접이식 UI)
- +(추가), 편집 아이콘 제공

| 카테고리 | 기본 제공 단어 |
|---|---|
| 기쁨 | 행복하다, 기쁘다, 즐겁다, 신나다, 뿌듯하다, 만족스럽다, 설레다 |
| 슬픔 | 슬프다, 우울하다, 속상하다, 서럽다, 허전하다, 그립다, 외롭다 |
| 분노 | 화나다, 짜증나다, 답답하다, 억울하다, 분하다, 열받다 |
| 불안 | 불안하다, 걱정되다, 초조하다, 긴장되다, 두렵다, 무섭다 |
| 기타 | 놀랍다, 당황스럽다, 창피하다, 무기력하다, 지치다 |

### 화면7 · 가이드
- 첫 실행 시 팝업으로 자동 표시, 이후 '?' 버튼으로 언제든 재열람 가능
- 사건 작성 예시 / 팁 
- 감정 작성 예시 / 팁
- 생각 작성 예시 / 팁 
- 이론적 근거 및 기대효과

## 7. 개발 순서

1. **데이터 구조 + 기본 CRUD** (스키마, 리포지토리, 기본 단어 시드, 테스트) — 완료
2. 일기 작성/보기 화면 (화면2, 3) — CustomTkinter로 완료
3. 목록/캘린더 화면 (화면4-1, 4-2) — CustomTkinter로 완료
4. **UI 프레임워크 전환 (26.07.12 추가)**: 화면2·3을 PySide6로 이식 → 한글 입력
   검증(사용자 확인) → 화면1·4 이식 → CustomTkinter 코드 제거
5. 감정 단어집 + 자동인식·자동완성 (화면6, 화면2 연동, conjugator/detector —
   서비스 로직·테스트는 구현 완료, UI 연동 남음)
6. 분석 화면 (화면5)
7. 가이드 + 온보딩 팝업 (화면7)
8. 백업/내보내기 기능 (화면1 연동) — 완료 (26.07.20~21, 네비게이션 바에서 진입)
9. UI 다듬기 + PyInstaller onedir 패키징 (LICENSES 동봉, zip 배포)
   — 완료 (26.07.24). 아래 §9 참고
10. macOS(Apple Silicon) 지원 + GitHub Actions 빌드
    — 완료 (26.07.28). 아래 §10 참고

- 중요한 단계마다 Git 커밋

## 8. 결정 기록 — 26.07.22 점검에서 나온 사항들

코드 점검에서 나온 다섯 건 중 둘(백업 복원 예외 누출, reportlab 유령 의존성)은
바로 고쳤고, 판단이 필요한 나머지 셋을 26.07.24에 아래와 같이 정했다.

### 8-1. DB 저장 위치 — 배포본은 `%LOCALAPPDATA%`

**문제**: `config.app_dir()`이 exe 옆을 가리켜, `C:\Program Files\`에 설치하면
쓰기 권한이 없어 첫 실행부터 DB를 만들지 못한다.

**결정**: 한 함수가 겸하던 두 역할을 나눴다.

- `config.data_dir()` — DB가 놓이는 곳. 배포본은 `%LOCALAPPDATA%\감정일기\`,
  개발 중에는 지금까지처럼 프로젝트 루트. 개발 경로를 바꾸면 쓰던 일기를 못
  찾게 되고 파일을 바로 열어 보기도 불편해서 그대로 두었다.
  (26.07.28에 macOS 갈래가 더해졌다 — §10-1)
- `config.export_dir()` — 내보내기·백업 대화상자의 기본 폴더. 문서 폴더를
  제안한다. 데이터 폴더는 숨은 경로라 내보낸 파일을 사용자가 찾지 못한다.

기존 DB를 옮기는 로직은 넣지 않았다. 배포본에는 이전 데이터가 없고 개발
환경은 경로가 그대로라, 옮길 대상이 애초에 없다.

⚠️ 개발 중 DB는 프로젝트 루트에 남으므로, 프로젝트 폴더가 OneDrive 안에 있는
동안에는 개발용 일기가 동기화된다. 배포본은 영향받지 않는다.

### 8-2. PDF·화면 폰트 — 시스템 폰트 + 대체 목록

**문제**: 맑은 고딕 하나만 지정하고 있어, 그 폰트가 없는 환경(비한국어
Windows·macOS·Linux)에서 한글이 깨진다.

**결정**: 폰트를 **동봉하지 않고**, 설치된 것 중 첫 번째를 쓰는 대체 목록
(`theme.FONT_STACK`)을 준다. 맑은 고딕 → Malgun Gothic → 나눔고딕 →
NanumGothic → Noto Sans KR → Apple SD Gothic Neo → sans-serif 순.

- 근거: 동봉하면 배포 용량이 늘고 OFL 고지 의무가 따라오는데, 실사용 환경
  (한국어 Windows)에는 맑은 고딕이 항상 있다. 없는 환경에서도 다른 한글
  폰트로 이어지므로 깨지지 않는다.
- 적용 지점을 한곳으로 모았다: `theme.qfont()` (Qt 위젯·PDF)와
  `theme.FONT_STACK_CSS` (QSS·내보내기 HTML). 전에는 `style.py`가 theme를
  거치지 않고 '맑은 고딕'을 직접 박아 두는 등 세 군데에 흩어져 있었다.
- 검증: 맑은 고딕을 뺀 목록으로도 나눔고딕으로 넘어가는 것과, 생성한 PDF에
  폰트가 임베드되고 겹받침까지 정상 렌더링되는 것을 확인했다.

### 8-3. DB 평문 저장 — 암호화하지 않는다 (문서로 고지)

**사실**: 일기는 암호화 없이 SQLite 파일에 저장된다. 그 파일을 열 수 있는
사람은 내용을 볼 수 있다.

**결정**: 암호화를 **구현하지 않고**, 그 사실과 대처법을 문서에 밝힌다.

- 근거: 암호화하려면 비밀번호 입력 UI, 분실 시 복구 불가 문제, 외부 의존성
  (SQLCipher)이 따라온다. 지금 외부 의존성은 PySide6 하나뿐이라는 것이 이
  프로젝트의 장점인데 그것을 잃는다. 위협 모델도 '같은 PC를 쓰는 타인'으로
  한정되는데, 이는 Windows 계정 분리로 이미 막을 수 있다.
- 고지 위치: `README.md`에 저장 위치·계정 분리 권장·백업 권장을 적었다.
- 대안 제시: 남에게 보여줄 목적이라면 DB 파일을 넘기지 말고 '보기·공유용
  내보내기'로 범위를 골라 문서를 만들도록 안내했다.


## 9. 패키징 구성 (26.07.24 완료)

`python tools/build.py` 한 번으로 아이콘 생성부터 zip까지 끝난다.

- **형태**: PyInstaller **onedir**. 단일 exe로 묶지 않는다 — Qt 라이브러리가
  `_internal/` 에 별도 파일로 남아야 LGPL v3의 '라이브러리를 교체할 수 있어야
  한다'는 조건을 지킬 수 있다. NOTICE.txt에도 이 점을 적었다.
- **이름**: exe·폴더 모두 `EmotionDiary` (영문). 경로·압축 문제를 피하려는
  것이고, 창 제목과 작업표시줄에는 `setApplicationDisplayName`으로 '감정일기'가
  나온다.
- **크기**: 86MB(압축 34MB). 앱이 쓰는 Qt 모듈이 QtCore·QtGui·QtWidgets
  셋뿐이라 나머지(QtWebEngine·QtQuick·Qt3D 등)를 spec의 excludes로 뺐다.
  결과적으로 `_internal/PySide6` 에는 .pyd가 4개만 남는다.
- **아이콘**: `tools/make_icon.py` 가 QPainter로 그린다(이미지를 가져다 쓰지
  않아 저작권 이슈가 없다). 세이지 그린 바탕에 회색 구름과 그 뒤의 노란 해.
  16~256px를 한 ICO에 담고, 16px에서도 형태가 뭉개지지 않도록 잔 장식은 뺐다.
  생성물이라 git에는 올리지 않는다.
- **자산 경로**: 패키징하면 `.py`가 아카이브로 묶여 `__file__` 기준 경로가
  실제 파일을 못 찾을 수 있다. `config.asset_path()` 가 frozen일 때
  `sys._MEIPASS` 를 기준으로 잡는다. QSS의 드롭다운 화살표 PNG가 여기에
  걸려 있어, 어긋나면 콤보 화살표가 통째로 사라진다.
- **라이선스**: `LICENSES/` 에 LGPL-3.0·GPL-3.0 전문과 직접 작성한
  NOTICE.txt. 전문은 Qt 소스 저장소(qt/qtbase)의 LICENSES 폴더에서 받는다 —
  우리가 재배포하는 것이 Qt이므로 Qt가 자기 배포본에 넣는 그 파일을 쓴다.
  (PySide6 wheel에는 LGPL 전문이 들어 있지 않다.)
- **동봉 문서**: `packaging/README.txt` — 실행법, 일기 저장 위치, 백업 권장,
  암호화하지 않는다는 고지, 지우는 방법.

### 검증한 것

빌드 성공과 실제 동작은 별개라 exe를 띄워 확인했다.

- exe 실행 → 창이 뜨고 콘솔 창은 함께 뜨지 않음, 제목·아이콘 정상
- DB가 `%LOCALAPPDATA%\감정일기\` 에 생성되고 개발용 DB는 건드리지 않음
- QSS가 배포본의 화살표 PNG를 가리키고 실제로 로드됨(20x20)
- 폰트가 한글 폰트로 해석되고, 내보낸 PDF에 폰트가 임베드됨
- zip을 프로젝트 밖(`C:\Temp`)에 풀어도 정상 실행 — 소스나 개발 환경에
  기대지 않음
## 10. macOS 지원 (26.07.28 완료)

Mac을 쓰는 사람도 이 프로그램을 쓸 수 있게 했다. **개발자에게 Mac이 없다**는
것이 이 작업의 전제이자 가장 큰 제약이었다 — 코드를 고치는 것보다 *Mac 없이
그것이 정말 도는지 확인할 방법*을 갖추는 쪽이 작업의 본질이었다.

**범위**: Apple Silicon(arm64)만. Intel Mac은 뺐다. 지금 나오는 Mac은 전부
Apple Silicon이고, 두 아키텍처를 다 지원하려면 빌드도 검증도 두 배가 된다.

### 10-1. 고친 곳 — 저장 경로 한 곳뿐

앱 코드 7,400줄 중 플랫폼에 매인 곳은 `config.data_dir()` 하나였다.
`LOCALAPPDATA` 환경변수를 읽는 그 자리에 `sys.platform == "darwin"` 갈래를
더했다.

| 실행 형태 | DB 위치 |
|---|---|
| 배포본 (Windows) | `%LOCALAPPDATA%\감정일기\` |
| 배포본 (macOS) | `~/Library/Application Support/감정일기/` |
| 소스로 실행 | 프로젝트 루트 |

`~/Library/Application Support`는 macOS가 앱 데이터를 두도록 정한 자리다.
`export_dir()`은 이미 `Path.home() / "Documents"`라 손댈 것이 없었다.

나머지가 이미 중립이었던 이유는 처음부터 그랬다기보다 쓰던 도구 덕이 크다.
경로는 전부 `pathlib`이고, 폰트 대체 목록에는 `Apple SD Gothic Neo`가 이미
들어 있었으며, 쓰는 Qt 모듈이 QtCore·QtGui·QtWidgets 셋뿐이라 macOS에서
빠지는 것이 없었다.

### 10-2. 패키징 — `.app` 번들과 ditto

Windows 쪽 설정은 건드리지 않고 갈래만 더했다.

- **아이콘**: macOS는 `.ico`를 못 읽는다. `tools/make_icon.py`가 같은 그림을
  `.icns`로도 저장한다. Qt가 `.icns` 쓰기를 지원해서 **Mac 없이 Windows에서
  만들 수 있었다** (파일 매직 바이트와 크기 헤더로 확인).
- **`.app` 번들**: spec에 `BUNDLE(...)`을 더했다. macOS에서는 폴더가 아니라
  `EmotionDiary.app` 형태여야 더블클릭으로 열린다. Info.plist에 한글 표시
  이름('감정일기')과 `bundle_identifier`를 넣었다. `version_info.txt`는
  Windows exe 속성 전용이라 macOS 분기에서는 넘기지 않는다.
- **압축은 `ditto`로**. 파이썬 `zipfile`로 `.app`을 담으면 실행 권한과
  심볼릭 링크가 날아가 받는 쪽에서 열리지 않는다. macOS 기본 도구인
  `ditto -c -k --sequesterRsrc --keepParent`는 그것을 보존한다.
- onedir 구성은 그대로다. `.app` 속도 결국 폴더라 Qt 라이브러리가 별도
  파일로 남고, LGPL의 '교체 가능성' 조건을 계속 충족한다.

### 10-3. Mac 없이 검증하기 — GitHub Actions

`.github/workflows/build.yml`에 두 갈래를 뒀다.

1. **테스트** — `windows-latest`와 `macos-latest` 양쪽에서 단위 테스트 137개.
   macOS 작업 때문에 Windows가 깨지지 않았는지 보는 안전망을 겸한다.
2. **macOS 빌드** — `macos-latest`(arm64)에서 `.app`을 만들고, 그것을 실제로
   띄워 본 뒤, zip을 아티팩트로 올려 내려받을 수 있게 한다.

빌드만 되고 실행은 안 되는 일이 흔해서, `tools/verify_app.py`를 새로 썼다.
서버에는 화면이 없으므로 `QT_QPA_PLATFORM=offscreen`으로 창을 만들고 13가지를
확인한다 — 저장 폴더가 그 OS의 표준 자리인지, 화살표 자산이 읽히는지, 창이
뜨는지, DB가 생기는지, 일기 저장·감정 태그가 되는지, 다섯 화면이 모두
전환되는지, 차트·HTML·PDF 내보내기가 나오는지, 다크 테마가 켜지는지.

**테스트는 push마다 자동, 빌드는 손으로 돌릴 때만** 한다. private 저장소에서
macOS 러너는 분당 10배로 계산되기 때문이다(월 무료 2,000분). public으로
전환하면 무료다.

첫 실행(26.07.28) 결과: 세 잡 모두 통과, 빌드 54초, `.app` 99MB / zip 35MB.
번들 안에 `_CodeSignature`가 생기는데, PyInstaller가 넣는 ad-hoc 서명이라
Apple 인증서와는 다르다 — Gatekeeper 경고는 그대로 뜬다.

### 10-4. 남는 한계 — 미리 적어 둔다

- **눈으로 보는 UI 확인은 못 한다.** 창 크기·글자 잘림·색의 조화 같은 것은
  실제 Mac 사용자의 피드백이 필요하다.
- Windows에서 offscreen으로 돌리면 시스템 폰트를 하나도 읽어 들이지 않아
  (`QFontDatabase.families()`가 0) 폰트 관련 항목은 판정할 수 없다.
  verify_app.py는 그 경우 SKIP으로 표시하고 넘어간다.
  **macOS 러너에서는 offscreen이어도 폰트가 올라와** 실제로 확인됐다
  (`Apple SD Gothic Neo`로 해석, PDF에 폰트 임베드).
- **서명이 없어 첫 실행에 경고가 뜬다.** Apple 개발자 등록(연 $99)을 하지
  않았다. 우클릭 → '열기'로 넘어갈 수 있고, README와 동봉 문서에 적었다.
- **Intel Mac은 범위 밖이다.**

### 검증한 것 (Windows 회귀)

macOS를 더하면서 Windows가 그대로인지가 가장 중요했다.

- 단위 테스트 137개 + 스모크 8종 통과
- `python tools/build.py` 재빌드 → exe 실행, 창 제목 '감정일기' 확인
- DB가 `%LOCALAPPDATA%\감정일기\` 에 그대로 생성됨
- 배포본 크기 86.0MB / 파일 65개 (`.icns`가 늘어 이전보다 1개 많다)


## 11. v1.0 공개 준비 (26.07.28)

### 11-1. 오픈소스로 공개 — MIT

PySide6가 LGPL이라 소스를 열어야 하는 것으로 오해하기 쉬운데, LGPL이
요구하는 것은 '라이브러리를 교체할 수 있게 하라'뿐이고 onedir 배포와
`LICENSES/` 동봉으로 이미 지켰다. 내 코드를 닫아도 문제가 없으니, 공개는
순수한 선택이었다.

**열기로 했다.** 이 프로젝트의 목적이 포트폴리오이기 때문이다. 닫으면
남는 것은 실행 파일 하나이고, 정작 보여 주고 싶은 것 — 왜 암호화하지
않았는지, 왜 폰트를 동봉하지 않았는지 같은 이 문서의 결정 기록 — 은
코드를 읽을 수 있을 때만 증거가 된다. 덤으로 public 저장소는 GitHub
Actions가 무료라 macOS 빌드 부담도 사라진다.

라이선스는 **MIT**. 짧고 널리 알려져 있어 '봐도 되고 가져다 써도 된다'는
뜻이 분명하다. `LICENSE` 파일이 없으면 public이어도 법적으로는 저작권
전부 보유라 아무도 쓸 수 없다 — 공개와 오픈소스는 다르다.

### 11-2. 정신건강 면책 문구

감정일기는 마음을 다루는 도구이고, 만든 사람은 심리학 전공자다. 의료기기가
아니라는 것과 위기 시 연락처(자살예방 상담전화 109, 정신건강 상담전화
1577-0199)를 **README·동봉 문서·앱 가이드 세 곳 맨 아래**에 넣었다.

앱에서는 가이드 팝업의 탭 바깥 맨 아래에 두었다. 탭 안에 넣으면 그 탭을
열지 않은 사람은 영영 보지 못한다.

### 11-3. 화면 스케치 PDF 제외

기획 단계의 손그림 PDF(6.6MB)를 추적에서 뺐다. 내용 자체는 문제가 없었지만,
공개 저장소에 둘 이유가 없는 데다 파일명으로 '설계용 PDF'와 '일기를 내보낸
PDF'를 가려내던 `.gitignore` 예외 규칙이 위험했다. 예외를 없애고 `*.pdf`를
전부 막았다.

### 11-4. 바탕화면 바로가기

배포본은 폴더 안의 실행 파일을 직접 찾아 눌러야 해서 불편하다. 첫 실행 때
가이드를 닫으면 바로가기를 만들지 물어보고, 이후에는 가이드의 '바탕화면
바로가기' 카드에서 언제든 만들 수 있다. 물어보는 것은 한 번뿐이다
(`meta` 테이블의 `shortcut_asked`).

- **Windows**: PowerShell의 `WScript.Shell`로 `.lnk`를 만든다. 파이썬만으로
  쓰려면 pywin32가 필요한데, 의존성을 하나 더 얹느니 Windows에 늘 있는
  PowerShell을 부르는 편이 가볍다. 창 없는 앱이므로 `CREATE_NO_WINDOW`로
  콘솔이 번쩍이지 않게 한다.
- **macOS**: `.app`을 가리키는 심볼릭 링크. Finder 별칭(alias)을 만들려면
  osascript로 Finder를 조종해야 하고, 그 순간 "'감정일기'가 Finder를
  제어하려 합니다" 권한 팝업이 뜬다. 링크는 그런 것 없이 더블클릭으로
  열린다.
- **바탕화면 위치**: Windows에서 바탕화면이 늘 `~/Desktop`인 것은 아니다.
  OneDrive를 쓰거나 한국어 환경이면 `~/OneDrive/바탕 화면` 등으로 옮겨져
  있다. 레지스트리(`User Shell Folders`)에서 실제 위치를 읽고, 못 읽으면
  `~/Desktop`으로 물러선다. 개발 PC에서 확인해 보니 실제로 OneDrive 아래
  한글 경로였다.
- 소스로 실행할 때는 가리킬 실행 파일이 없어 버튼과 팝업이 모두 나오지
  않는다(`shortcut.is_supported()`).
